R2PM_BEGIN

R2PM_GIT "https://github.com/radare/r2pipe.go"
R2PM_DESC "[syspkg-r2pipe] r2pipe API for Go"

R2PM_INSTALL() {
	go get github.com/radare/radare2
	go get github.com/radare/r2pipe.go 
	go get github.com/radare/radare2/radare2-bindings/ 
	cd ${GOPATH}/src/github.com/radare/radare2-bindings/ 
	cd go && make || exit 1
	mkdir ${GOPATH}/src/r_bin || exit 1 
	cp * ${GOPATH}/src/r_bin/ || exit 1
}

R2PM_UNINSTALL() {
	rm -rf ${GOPATH}/src/r_bin
	rm -rf ${GOPATH}/src/github/radare/r2pipe.go
	rm -rf ${GOPATH}/src/radare/radare2/
}
